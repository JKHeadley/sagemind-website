# Gmail Configuration for Contact Form
# =====================================

# The Gmail address to send from (your Gmail account)
GMAIL_USER=your-email@gmail.com

# Gmail App Password (NOT your regular password!)
# Generate at: https://myaccount.google.com/apppasswords
# Requirements: 2FA must be enabled on the Google account
GMAIL_APP_PASSWORD=xxxx-xxxx-xxxx-xxxx

# Email address to receive contact form submissions
# (defaults to GMAIL_USER if not set)
CONTACT_EMAIL=info@sagemindai.io


# Google Calendar Configuration for Scheduling
# =============================================

# Google Calendar ID to check availability and create events
# Usually your email address, or find in Calendar Settings > Integrate calendar
GOOGLE_CALENDAR_ID=your-email@gmail.com

# Google Service Account credentials (JSON format, single line)
# Setup instructions:
# 1. Go to Google Cloud Console: https://console.cloud.google.com
# 2. Create a new project (or select existing)
# 3. Enable "Google Calendar API" in APIs & Services > Library
# 4. Go to APIs & Services > Credentials
# 5. Create Credentials > Service Account
# 6. Give it a name and create
# 7. Click on the service account > Keys > Add Key > Create new key > JSON
# 8. Copy the ENTIRE JSON content here as a single line
# 9. IMPORTANT: Share your calendar with the service account email
#    (Go to Google Calendar > Settings > Share with specific people > Add the service account email)
GOOGLE_SERVICE_ACCOUNT_KEY={"type":"service_account","project_id":"...","private_key":"...","client_email":"..."}

# Timezone for scheduling (IANA format)
TIMEZONE=America/Los_Angeles
